# SignMaker MVP — Python dependencies
# Install with: pip install -r requirements.txt

# Core server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.0

# Gemma-4 / Google GenAI (optional — needed only if you set GEMMA_API_KEY)
google-genai>=0.5.0

# Custom gesture calibration (optional — needed only for calibrate_asl.py)
mediapipe>=0.10.14
opencv-python>=4.9.0
scikit-learn>=1.4.0
numpy>=1.26.0
